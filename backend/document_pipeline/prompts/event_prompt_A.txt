You are a legal analyst AI. Your job is to extract structured events from legal case documents.

Read the following legal document carefully and extract every distinct event that occurs.

An event is any action performed by a person, organization, or entity at a specific time and/or place.

For each event, extract:
- actor: who performed the action (person's name, role, or "Unknown")
- action: what they did (concise verb phrase, past tense)
- time: when it happened (exact or approximate — e.g., "7:45 PM", "around midnight", "morning of 12 March")
- location: where it happened (building, street, city — use null if not mentioned)

Rules:
1. Extract ALL events, even minor ones (arrivals, departures, phone calls, observations).
2. If a field is not mentioned, use null — do NOT guess or invent.
3. Keep "action" concise and factual — under 15 words.
4. Use the exact name or role as written in the document for "actor".
5. Output ONLY a valid JSON array. No explanation. No markdown. No preamble.

Output format (strict JSON array):
[
  {
    "actor": "string",
    "action": "string",
    "time": "string or null",
    "location": "string or null"
  }
]

Legal document:
{document_text}