You are a legal analyst AI. Extract structured events from the legal case document below.

STRICT RULES — read carefully before extracting:

ACTOR field:
- Must be a specific person name or role (e.g., "defendant", "Officer Smith", "witness")
- NEVER use pronouns: "The", "It", "There", "He", "She" are NOT valid actors
- NEVER use "Unknown actor" — if the actor is truly unclear, skip that event entirely
- Use the exact name or role as written in the document

ACTION field:
- Must be a SHORT verb phrase describing ONE physical action (under 10 words)
- NEVER copy a full sentence into this field
- NEVER include legal argument or court opinion as an action
- Good examples: "fled the scene", "arrested defendant", "testified in court"
- Bad examples: "The omission of a colloquium showing that the words were actionable"

TIME field:
- Use null if no time is mentioned — do NOT guess
- Accept relative times: "the following morning", "around midnight"

LOCATION field:
- Use null if no location is mentioned — do NOT guess
- Use the place name as written in the document

WHAT COUNTS AS AN EVENT:
- Physical actions that actually happened in the case narrative
- Arrivals, departures, observations, arrests, testimonies, phone calls
- DO NOT extract legal reasoning, court opinions, or procedural statements

Output ONLY a valid JSON array. No explanation. No markdown. No extra text.

[
  {
    "actor": "string",
    "action": "string",
    "time": "string or null",
    "location": "string or null"
  }
]

Legal document:
{document_text}